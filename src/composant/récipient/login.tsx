import React from react


type LoginComponentProps = {
 children : React.reactNode
} 

const LoginComponent = ({children}:LoginComponentProps) =>{
Return (
<div>{children} </div>
) 
}

export default LoginComponent;
